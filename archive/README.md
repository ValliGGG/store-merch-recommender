# archive/

One-off / exploratory scripts and backups from the SK build-out (April 2026):
brand probes, homepage-tile tweaks, search-redirect deploys, promo audits,
variant-stock filters, theme-dump helpers, and pre-change `_index_backup*.json`
snapshots.

**Not part of the production pipeline.** The live, scheduled pipeline is
`scripts/01..08` driven by `.github/workflows/weekly_refresh.yml`. Nothing in
`scripts/` imports anything here. Kept for reference / history; safe to delete
once no longer useful.
